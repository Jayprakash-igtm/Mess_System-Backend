specVersion: "v1beta"
serviceId: "hostlemess"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "hostlemess-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
